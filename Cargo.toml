[package]
name = "oauth2-login-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
curl = "0.4"
reqwest = { version = "0.12", features = ["json", "blocking"] }
